CREATE TABLE IF NOT EXISTS peca (
    Pcod INT NOT NULL  PRIMARY KEY,
    Pnome VARCHAR(30) NOT NULL,
    COR VARCHAR(30),
   PESO DECIMAL(10, 2), 
     Ccod INT,
     FOREIGN KEY(Ccod) REFERENCES Cidade   
);

CREATE TABLE IF NOT EXISTS peca (
    PRcod INT NOT NULL  PRIMARY KEY,
    PRnome VARCHAR(30) NOT NULL,
     Ccod INT,
     FOREIGN KEY(Ccod) REFERENCES Cidade   
);
CREATE TABLE IF NOT EXISTS FORNECEDOR (
    Fcod INT NOT NULL  PRIMARY KEY,
    Fnome VARCHAR(30) NOT NULL,
    Status VARCHAR(20) DEFAULT 'ATIVO',
    FONE INT,
     Ccod INT,
     FOREIGN KEY(Ccod) REFERENCES Cidade   
);


CREATE TABLE IF NOT EXISTS FORNECIMENTO (
    ID INT PRIMARY KEY,
      Fcod INT,
     Ccod INT,
       PRcod INT,
     FOREIGN KEY(Ccod) REFERENCES Cidade,   
     FOREIGN KEY(Fcod) REFERENCES FORNECEDOR,   
     FOREIGN KEY(PRcod) REFERENCES PROJETO,
       QUANTIDADE INT 

);

ALTER TABLE  PROJETO 
DROP FOREIGN KEY ICODIGO

ALTER TABLE  PROJETO 
ADD CONSTRAINT FK_CCOD
FOREIGN KEY (CCOD) REFERENCESTBL_CIDADE (CCOD)

ALTER TABLE  FORNECEDOR 
ALTER COLUMN FONE TYPE BIGINT

